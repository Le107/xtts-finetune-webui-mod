1. Добавлена скорость речи.
2. Добавлено авторесемплирование сгенерированой речи под оригинал(Пример: оригинал: 48000Hz, ген.речь:24000 > 48000 Hz. Оригинал по умолчанию это эталонный звук динамика: `reference.wav`. Либо указанный другой.). Также определяет каналы - стерео или моно.
3. Сохранение в папку 'output', с темже именем файла, что и эталонный звук динамика(по умолчанию: `output\reference.wav`. Либо указанный другой).
4. Переведено на русский.
5. config:
   ```docker
    compute_f0": true,
    compute_energy": true,
    compute_linear_spec": true,
    use_speaker_weighted_sampler": true,
    use_grad_scaler": true,
    use_noise_augment": true,
   ```
7. Автозавершение обучения если тренировка начала выдавать худшие резултаты.
8. Лучшая модель сохраняется только в конце обучения, а не перезаписывается каждую эпоху. А чекпоинт каждые 10 эпох(один файл, пердыдущий удаляется). Это экономит время и не изнашивает жесткий диск. Сохраняется сразу оптимизированная модель в папке `ready`. Также чекпоинт: [+] плюс: сохраняется оптимизированным; [-] минус: нельзя продолжить обучение, только начать сначала как с базовой или обученой моделью.
9. Добавлены итерации(повтор) обучения. Если в конце обучения(первая итерация) avg_loss больше 3, то берутся все записи из `metadata_train.csv` и `metadata_eval.csv` из `dataset`, перемешиваются в случайном порядке(остается соответсвие: wavs/'аудио'.wav|текст) и снова распределяются в пропорции 85/15% в этих файлах, вместо базовой модели берется сохраненная модель предыдущей итерации(при первой итерации - базовая модель или пользовательский файл model.pth), и обучение запускается снова(следующая итерация). И так 10 итераций или пока avg_loss не станет меньше 3.
10. Создание датасета по субтитрам. (см. в finetune_models https://github.com/Le107/xtts-finetune-webui-mod/blob/main/finetune_models/readme.md). Когда нарезается датасет по субтитрам создается файл `Dialogs.txt`, в котором сохраняется `имя_аудиофайла_датасета`=`текст_из_субтитров`.
11. Добавлен скрипт перевода `Dialogs.txt` на другой язык - `translate_dialogs.py` (см. в finetune_models). Сохраняет перевод отдельным файлом `Dialogs_*.txt`, где * - язык, на который переводит(en, ru,... или другой).
12. Добавлен пакетный способ генерации, где вместо reference.wav(эталоный звук динамика) файл берет из датасета, а текст из `Dialogs.txt`(или `Dialogs_*.txt`, по выбору), и сохраняет в папку `translated_output`.
13. Добавлен скрипт `add_stress_manual.py`, и небольшой словарь `custom_dict.txt`. В словарь можно добавлять слова с правильным удареним на гласные, а скрипт, с помщью словаря, правит `Dialogs_ru.txt` и сохраняет отдельно `Dialogs_ru_stressed.txt` (см. в finetune_models).

Установка: скопировать всё с заменой на установленный репозиторий от daswer123 (daswer123/xtts-finetune-webui) и еще добавить `ffmpeg.exe` в корневую папку. Можно установить от сюда (`install_cpu.bat` и `install_gpu.bat`), но перед установкой убедитесь, что у вас отсутствуют(удалены) папки `TTS` и `trainer` по пути `..\venv\Lib\site-packages\`, потом проводите установку, после установки копируйте папку `venv` из этого репо поверх с заменой.


Не работает с torch cuda из-за ошибки https://github.com/daswer123/xtts-finetune-webui/issues/24 и https://github.com/SYSTRAN/faster-whisper/issues/42. Поэтому в коде xtts_demo.py изменено float16 на float32. При таком коде исползуйте install_cpu.bat
```docker
                        # Detect compute type 
                        if torch.cuda.is_available():
                            compute_type = "float32"
                        else:
                            compute_type = "float32"
```
Это только Whisper, в остальном можно попробовать использовать install_gpu.bat и в коде сменить на float16 или int8 (в этом разница xtts_demo_cpu.py и xtts_demo_gpu.py)
```docker
                    if torch.cuda.is_available():
                        compute_type = "int8"
                    else:
                        compute_type = "float32"
```

Проблемы с ударением? Используйте "таблицу символов": а - á, я - já, э - é, e - jé, ы - ы´. Также помогает разделение, пробелом или тире(минус), частей слова(приставки, корни, окончания, по слогам(после каждой гласной, на которую ставит(не нужное) ударение, ставим тире(а лучше _ - Shift+-, нижнее подчёркивание)). Можно попробовать разделить две подряд идущие согласные, от этого тоже может зависить ударение. Всё - в-се. Крайний случай: отделить слово запятыми.

Если в вопросительных предложениях нужно поставить ударение вопроса на конкретное слово, ставим вокруг него нижнее подчеркивание (Пример: "Это тебе было нужно?" - "ˍЭтоˍ тебе было нужно?"), запятые не всегда помогают. Если не сработает, можно попробовать так: "Это? тебе было нужно", ну а паузу обрезать в редакторе, или перед словом поставить частицу "не"(Варианты: "Не это тебе было нужно?", "Это не тебе было нужно?", "Это тебе не было нужно?", "Это тебе было не нужно?") и потом также ее удалить в редакторе.

Еще, чтобы сделать идеально один-в-один, можно прогнать через RVC. Кто-то скажет "обучать на английском датасете - появляется английский акцент", что даже ни индекс на русском датасете, ни pretrained русские модели, не помогают, т.к. английский датасет все "съедает". Это лечится самим датасетом, рассказываю как: 
Сначала, как обычно, обучаете модель на английском датасете, где обязательно появится акцент, накладываете на оригинал(от xtts) и слушаем на каких словах акцент. Эти слова вырезаем из оригинала русской речи(именно из тех аудиофайлов, которые собираетесь пропускать через RVC) и кидаем в английский датасет, если вся фраза, все слова с акцентом, то всю фразу кидаем и дообучиваем модель. Сами слова "размажутся" английским датасетом, а русские "звуки", которых нет в английском останутся и модель будет "говорить" чисто на русском.
